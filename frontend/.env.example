# In development these can be left unset — Vite's proxy handles routing to the backend.
# In production (frontend and backend on different domains) set both:

VITE_API_URL=https://api.yourdomain.com/api
VITE_SERVER_URL=https://api.yourdomain.com
